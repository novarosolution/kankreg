import React from "react";
import StorefrontCampaignHero from "./StorefrontCampaignHero";

/** Desktop/mobile-web home hero — campaign slider + circular categories. */
export default function WebPremiumHero({ navigation }) {
  return <StorefrontCampaignHero navigation={navigation} />;
}
