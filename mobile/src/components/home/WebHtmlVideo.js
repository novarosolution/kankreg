import React from "react";
import { View } from "react-native";

/** Native stub — web uses `WebHtmlVideo.web.js`; story sections use `CinemaStoryPlayer`. */
export default function WebHtmlVideo({ style }) {
  return <View style={style} />;
}
