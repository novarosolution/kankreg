# KankreG Mobile

Expo React Native app — iOS, Android, and web.

```bash
npm install
cp .env.example .env
npm run start
```

Scripts: `start`, `web`, `android`, `ios`, `export:web`, `lint`

EAS builds: `eas build --platform android --profile preview`

Parent repo README: `../README.md`
